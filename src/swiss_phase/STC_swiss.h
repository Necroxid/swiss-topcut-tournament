#ifndef STC_SWISS_H
#define STC_SWISS_H

#include "../setup/STC_setup.h"

//Create the empt list for players
void createEmptyPlayers(players &_players);
//Create the empt list for teams
void createEmptyTeams(teams &_teams);
//Create the empt list for matches
void createEmptyMatches(matches &_matches);

#endif